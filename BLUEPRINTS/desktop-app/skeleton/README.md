# Desktop App Skeleton

Production-oriented desktop application skeleton with module boundaries.

## Structure

- `src/modules/*` feature modules
- `src/shared` reusable cross-cutting logic
- `tests/unit`, `tests/integration`
- `docs/adr` architecture decisions
