# Web App Skeleton

Production-oriented web application skeleton organized by feature modules.

## Structure

- `src/modules/*` feature modules
- `src/app` app shell and routing
- `src/shared` common reusable components/utilities
- `tests/unit`, `tests/integration`
- `docs/adr` architecture decisions
- `infra` deployment artifacts
