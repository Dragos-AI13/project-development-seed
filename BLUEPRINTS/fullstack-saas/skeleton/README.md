# Fullstack SaaS Skeleton

Production-oriented fullstack SaaS skeleton with tenant-aware module boundaries.

## Structure

- `src/modules/*` feature modules
- `src/shared` common contracts and helpers
- `tests/unit`, `tests/integration`
- `docs/adr` architecture decisions
- `infra` deployment and runtime artifacts
