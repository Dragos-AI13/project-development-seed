# Backend API Skeleton

Production-oriented backend API skeleton with modular structure.

## Structure

- `src/modules/*` feature modules
- `src/shared` common utilities and contracts
- `tests/unit`, `tests/integration`
- `docs/adr` architecture decisions
- `infra` deployment and runtime artifacts
