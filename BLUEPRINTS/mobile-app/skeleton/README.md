# Mobile App Skeleton

Production-oriented mobile application skeleton with modular features.

## Structure

- `src/modules/*` feature modules
- `src/shared` reusable UI and helpers
- `tests/unit`, `tests/integration`
- `docs/adr` architecture decisions
